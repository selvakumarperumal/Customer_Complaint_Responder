[project]
name = "ccr-poller"
version = "0.1.0"
description = "Customer Complaint Responder — IMAP Poller microservice"
requires-python = ">=3.12"
dependencies = [
    "imap-tools>=1.8.0",
    "redis>=5.0",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
]
